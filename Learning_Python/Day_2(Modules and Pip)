#Modules == they are used to borrow the code written by others and use it in our system.
#There are two types of modules:
#1. Built-in modules: These are modules that come with Python by default.
#2. External modules: These are modules that are installed separately using pip.
#to install module write code in terminal: pip install module name
#to use module we have to import it first using import keyword. like import module name
import pandas #pandas is a external module
import hashlib #hashlib is a built-in module
